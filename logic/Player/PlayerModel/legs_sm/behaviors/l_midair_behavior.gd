extends LegsBehavior

# func _ready() -> void:
# 	supported_actions = [
# 		LS.legs_action_midair
# 		]

# func update(input: InputPackage, delta: float) -> void:
# 	legs_sm.current_action.update(input, delta)
