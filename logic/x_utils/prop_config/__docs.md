## Contains static data classes for different configs 

Simple models which don't have complex logic

### Some wrap basic built in nodes properties
	E.g.: rigid physics, standard mat emisson.

### Other cover basic reusable things 
	E.g.: blend config (fade in/hold/fade out)
