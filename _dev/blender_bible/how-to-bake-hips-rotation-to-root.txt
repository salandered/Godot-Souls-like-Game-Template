const aramture name
const anim name
const result anim name



- copy anim and name result anim name
- link armature and result anim name
- select Edit mode for armature
- select hips bone and alt+P clear parent
- pose mode, select root, add copy Rotation constraint for bone. 
	 - target = Our armature and bone Hips
	 - axis Z (only Z, no inverse). 
	 - World spaces for both. (no inverted axis selected)
- bake action (root still selected): 
	- frames = anim frames, 
	- checked: only selected bones, visual keying, clear constraints, overwrite curr action. 
	- bake data: Pose. 
	- Channel: all or rotation. by default was all and it worked

check: root rotates according to anim. not fast. anim works also as expected. 
	(global anim rotation could ve been changed after alt P ). no crazy fast things


- parent hips to root (as they were). Edit mode, Select hips, root, Ctrl+P keep offset
- pose mode, select Hips
- Copy Rotation from Root, only Z! no inverse. everything else defaulted.

check: animation look as it should be, no nuances.

- bake (Hips still selected in Pose mode)


=== TROUBLESHOOTING ===

if ORIGINAL animation suddenly breaks
   - its not anim or armature. 
   - After playing anim with real ROOT ROTATION, root stucks in some specific position. it breaks other animations. 
   - just reset the pose before playing other animations on an armature which was playing root rotation. 
   - (kinda same wtih root location, but in that case the influence of prev anim is obvious. while here it can scare the crap out of u)
